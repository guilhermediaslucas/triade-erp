# Conexao do banco (NUNCA commitar o .env real - so este .example)
DB_URL=postgresql://usuario:senha@host/banco?sslmode=require
# Porta da API
API_PORT=3333
# Ambiente: development | production
NODE_ENV=development
